# BhagyaAI — Python Dependencies

# Core framework
fastapi==0.111.0
uvicorn[standard]==0.30.0

# Swiss Ephemeris (Vedic astrology calculations)
pyswisseph==2.10.3.2

# Database
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.1

# Pydantic (data validation)
pydantic==2.7.1
pydantic-settings==2.2.1

# Auth — JWT + bcrypt (passlib removed: version conflict with bcrypt 4.x on Python 3.12)
python-jose[cryptography]==3.3.0
bcrypt==4.1.3

# HTTP client
httpx==0.27.0

# Timezone handling
pytz==2024.1

# Environment variables
python-dotenv==1.0.1

# PDF generation
reportlab==4.1.0

# Dev / testing
pytest==8.2.0
pytest-asyncio==0.23.7
